# Copy to .env and fill in real values. Never commit .env to git.

# Phase 1 — required at launch
SUPABASE_URL=
SUPABASE_DB_STRING=
PORT=3000

# Phase 2 — prediction markets + economic data
KALSHI_API_KEY=
FRED_API_KEY=

# Phase 4 — AI Consensus, signal briefings, VIX driver, pulse surveys
ANTHROPIC_API_KEY=

# Phase 3 (optional, post-launch) — real APIs replace Claude-simulated estimates
OPENAI_API_KEY=
GOOGLE_AI_KEY=
MISTRAL_API_KEY=
DEEPSEEK_API_KEY=
PERPLEXITY_API_KEY=
